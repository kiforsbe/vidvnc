<?xml version="1.0" encoding="utf-8"?>
<!-- Template for the VidVNC MSIX; packaging/windows/build.mjs fills every double-braced value. -->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  xmlns:desktop6="http://schemas.microsoft.com/appx/manifest/desktop/windows10/6"
  IgnorableNamespaces="uap rescap desktop6">
  <Identity Name="{{name}}" Publisher="{{publisher}}" Version="{{version}}" ProcessorArchitecture="x64" />
  <Properties>
    <DisplayName>{{displayName}}</DisplayName>
    <PublisherDisplayName>{{publisherDisplayName}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <!-- Settings and logs stay in the real %LOCALAPPDATA%\VidVNC: shared with the CLI server
         and kept on uninstall. Requires the unvirtualizedResources capability. -->
    <desktop6:FileSystemWriteVirtualization>disabled</desktop6:FileSystemWriteVirtualization>
  </Properties>
  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="{{minimumOSVersion}}" MaxVersionTested="{{minimumOSVersion}}" />
    <PackageDependency Name="{{windowsAppRuntimeName}}" MinVersion="{{windowsAppRuntimeVersion}}" Publisher="{{windowsAppRuntimePublisher}}" />
  </Dependencies>
  <Resources>
    <Resource Language="en-us" />
  </Resources>
  <Applications>
    <Application Id="App" Executable="{{executable}}" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements DisplayName="{{displayName}}" Description="{{description}}" BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png" Square44x44Logo="Assets\Square44x44Logo.png" />
    </Application>
  </Applications>
  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
    <rescap:Capability Name="unvirtualizedResources" />
  </Capabilities>
</Package>
