# macOS distribution tests

Own clean-machine launch, signing/notarization validation, permission onboarding,
bundle relocation, upgrade/removal and architecture checks here. Use disposable
macOS test environments or explicitly authorized devices for machine-changing tests.
These checks do not replace native app/module tests or real-device streaming tests.
